/* FUTURE USE — uncomment when screens adopt this hook.
import { useUser } from "@/context/UserContext";

export function useAuth() {
  const { user, loading, refreshUser, signOut } = useUser();
  return { user, loading, refreshUser, signOut };
}
*/

export {};
